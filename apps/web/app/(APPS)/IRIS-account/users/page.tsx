import { Metadata } from "next";

export const metadata: Metadata = {
    title: "IRIS Account | Users",
    description: "IRIS Account Users",
};

export default function Page() {
    return (
        <>
            page
        </>
    )
}
import { Metadata } from "next"

export const metadata: Metadata = {
  title: "IRIS List | Media > Books",
  description: "Book media on IRIS List",
}

export default function Page() {
  return <>page</>
}
